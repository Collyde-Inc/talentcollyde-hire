---
description: Generate the interview kit — question blocks and listening cues tied to the rubric
argument-hint: "<role title or slug>"
---

Load the `interview-kit` skill and follow its instructions for the role in $ARGUMENTS.

Read `interview_rubric.md` for the role. Produce `interview_kit.md` with question blocks mapped to rubric dimensions, listening cues for each anchor, and anti-cheat language for stories the model has heard before. The kit must enable a screener to run a 45–60 minute call cold. No Observations section in the kit per `references/observations.md`.
